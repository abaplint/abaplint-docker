name: Docker Image CI

on:
  push:
    branches:
      - 'releases/*' # just as a placeholder now

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - name: Build the Docker image
    - uses: actions/checkout@v1
      run: docker build . --file Dockerfile --tag abaplint/abaplint:latest
